apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: front-end-argo-application
  namespace: argocd
spec:
  project: default

  source:
    repoURL: https://github.com/MaelMainsard/microapp-deploy
    targetRevision: HEAD
    path: ./
  destination: 
    server: https://kubernetes.default.svc
    namespace: front-end-pod

  syncPolicy:
    syncOptions:
    - CreateNamespace=true

    automated:
      selfHeal: true
      prune: true
