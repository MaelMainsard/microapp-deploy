apiVersion: v1
kind: Service
metadata:
  labels:
    app: order-service
  name: order-service
  namespace: microapp-deploy
spec:
  type: NodePort
  ports:
  - port: 3002
    protocol: HTTP
    targetPort: 3002
  selector:
    app: order-service
